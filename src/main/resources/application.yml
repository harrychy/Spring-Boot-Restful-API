spring:
  datasource:
    url: jdbc:postgresql://127.0.0.1:5432/tvseries?stringtype=unspecified&ApplicationName=Spring-Boot-Restful-API
    username: postgres
    password: sql
    driver-class-name: org.postgresql.Driver
  jackson:
    data-format: yyyy-MM-dd
    timezone: GMT+8
    serialization:
      write-dates-as-timestamps: false #ʹ����ֵtimestamp��ʾ����

mybatis.configuration.mapUnderscoreToCamelCase: true

logging:
  file: target/app.log
  level:
    ROOT: WARN
    com.example: TRACE